# 任务结果通知 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 日常任务跑完时，右上角常驻计数条实时显示 成功/跳过/异常 条数，并发一条系统通知列出跳过与异常的任务及原因。

**架构：** 三层单向依赖。`daily.ts` 的 `doTask` 只发布事件到 `taskBus`；`taskReport` 订阅事件累加汇总并生成纯文本文案；`taskNotifier` 订阅事件把汇总渲染到右上角悬浮计数条、在整轮结束时发 Android 系统通知。`daily.ts` 的 import 面只有两个符号（`发布` + `初始化通知`）。

**技术栈：** TypeScript（`target: es5`，Rhino 兼容，禁箭头函数/const/解构）→ Webpack 5 → `dist/*.js` → AutoX.js 6.6.7（Android 13 / API 33）真机运行。

**规格：** [docs/superpowers/specs/2026-09-19-任务结果通知-design.md](../specs/2026-09-19-任务结果通知-design.md)

**测试约定（重要）：** 本仓库没有 jest/mocha，测试 = 新增一个 webpack entry → 构建出 `dist/*.js` → 在设备上运行 → 回读日志。断言风格照抄 `test/skill-test.ts` 的 `assert(条件, 标签)` + 末尾 `summary()`。

**每个任务结束都要跑的通用命令：**

```bash
npm run build
```

预期：无报错，`dist/` 下生成对应产物。构建失败就先修，别往下走。

---

## 设备执行机制（已实测跑通，照抄即可）

设备 `55`（192.168.138.201:9319）在线。设备上的项目根是 **`/sdcard/脚本/xjskp`**（不是中文路径）。**设备侧没有任何自动同步**，构建产物必须推上去，分两步：

**第 1 步：推送构建产物（一条 curl，不走上下文）**

设备端的 helper server 有通用文件上传接口 `POST /upload?dir=&name=&overwrite=1`（实现见 `f:\ideaProject\autojs-helper\app\connection.py:292-328`，是纯字节写入，docstring 写"上传图片"只是主要用途）。直接在 Git Bash 里：

```bash
cd "f:/ideaProject/向僵尸开炮/xjskp" && curl -s -X POST --data-binary "@dist/任务通知诊断.js" \
  "http://192.168.138.201:9319/upload?dir=%2Fsdcard%2F%E8%84%9A%E6%9C%AC%2Fxjskp%2Fdist&name=%E4%BB%BB%E5%8A%A1%E9%80%9A%E7%9F%A5%E8%AF%8A%E6%96%AD.js&overwrite=1"
```

预期返回 `{"success":true,"path":"/sdcard/脚本/xjskp/dist/任务通知诊断.js"}`。

**必须校验字节数**（`dir` / `name` 是 URL 编码的，换文件名时要一起换编码）：

```bash
ls -l dist/任务通知诊断.js | awk '{print "local:", $5}'
```

再用 `exec_code` 查设备侧：

```js
JSON.stringify({ 设备字节: new java.io.File('/sdcard/脚本/xjskp/dist/任务通知诊断.js').length() })
```

两边不一致就是传坏了，重推。**这个通道对 306 KB 的 `main.js` 同样有效**（任务 7 用它）。

**第 2 步：在设备上执行并回读输出**

`engines.execScriptFile` 的输出回不来，改用 `eval` + 覆写日志函数（已验证 `console.log` 与 `log` 都能捕获）：

```js
var 行 = []
var 原consoleLog = console.log
console.log = function () { 行.push(Array.prototype.slice.call(arguments).join(' ')) }
try { global.log = function () { 行.push(Array.prototype.slice.call(arguments).join(' ')) } } catch (e) { }
try {
  eval(files.read('/sdcard/脚本/xjskp/dist/任务通知诊断.js'))
} catch (e) {
  行.push('执行抛错: ' + e)
}
console.log = 原consoleLog
行.join('\n')
```

预期：返回完整诊断日志，末尾是 `结果: N/N 通过`，`N` 与本计划标注的一致，且没有 ❌。

**exec 上下文的能力边界（已实测）**：`ui.run` 可用、`floaty.window` 建窗/设文字/关窗可用 → 计数条能自验。`notice` **不存在**（AutoX.js 无此 API），通知走原生 `NotificationManager`。

> **注意**：`eval` 在 exec 上下文里跑，与「AutoX.js 打开脚本执行」不完全等价（例如 `console.setGlobalLogConfig` 的落盘行为在两种环境下不同）。**任务 1-5 用 eval 自验没问题；任务 7 的整轮日常回归必须用真实脚本运行方式，那一步由 k 操作。**

---

### 任务 1：事件总线与诊断脚手架

建立事件契约、诊断脚本骨架、构建入口。这是后面所有任务的验证通道，必须先通。

**文件：**
- 创建：`src/utils/taskBus.ts`
- 创建：`test/任务通知诊断.ts`
- 修改：`webpack.config.js:6-13`（entry 加一行）

- [ ] **步骤 1：编写失败测试（诊断脚本骨架 + 总线用例）**

创建 `test/任务通知诊断.ts`：

```ts
// test/任务通知诊断.ts — 诊断"任务结果通知"的事件总线、计数、文案与渲染
// 运行:推到设备 /sdcard/脚本/xjskp/dist/ 后用 exec 上下文 eval 执行,回读日志
// 纯逻辑部分自动断言;末尾渲染冒烟部分需人眼确认
import { 发布, 订阅, 任务事件 } from '../src/utils/taskBus'

var total = 0
var pass = 0

function assert(cond: boolean, label: string): void {
  total++
  if (cond) {
    pass++
    console.log('✅ ' + label)
  } else {
    console.log('❌ ' + label)
  }
}

function summary(): void {
  console.log('')
  console.log('================================')
  console.log('   结果: ' + pass + '/' + total + ' 通过')
  console.log('================================')
}

console.log('')
console.log('===== 任务结果通知诊断 =====')

// ---------- 事件总线 ----------
// 注意:只用订阅返回的句柄摘掉自己注册的监听器,不做全局清空
// ——taskReport/taskNotifier 在模块体里注册订阅,全局清空会把它们一起抹掉
console.log('')
console.log('--- 总线: 订阅与发布 ---')
var 收到: string[] = []
var 取消A = 订阅(function (e: 任务事件): void { 收到.push('订阅A:' + e.类型) })
var 取消B = 订阅(function (e: 任务事件): void { 收到.push('订阅B:' + e.类型) })
发布({ 类型: '本轮开始' })
assert(收到.length === 2, '两个订阅者各收到一次, 实际 ' + 收到.length)
assert(收到[0] === '订阅A:本轮开始' && 收到[1] === '订阅B:本轮开始', '按注册顺序同步调用, 实际 [' + 收到.join(' | ') + ']')

取消A()
取消B()
发布({ 类型: '本轮开始' })
assert(收到.length === 2, '取消订阅后不再收到, 实际 ' + 收到.length)

console.log('')
console.log('--- 总线: 取消只影响自己 ---')
var 留着的 = 0
var 临时 = 0
var 取消临时 = 订阅(function (): void { 临时++ })
var 取消留着 = 订阅(function (): void { 留着的++ })
取消临时()
发布({ 类型: '本轮开始' })
assert(留着的 === 1 && 临时 === 0, '取消一个订阅者不影响另一个, 实际 留着=' + 留着的 + ' 临时=' + 临时)
取消留着()

console.log('')
console.log('--- 总线: 订阅者异常向上冒泡(契约行为) ---')
var 取消异常 = 订阅(function (): void { throw new Error('订阅者故意抛错') })
var 冒泡 = false
try {
  发布({ 类型: '本轮开始' })
} catch (e: any) {
  冒泡 = true
}
assert(冒泡, '订阅者抛错会冒泡到发布方(所以订阅者必须自行 try/catch)')
取消异常()

summary()
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npm run build`

预期：**FAIL** —— 构建报错，`Module not found: Error: Can't resolve '../src/utils/taskBus'`（此时 `taskBus.ts` 还不存在）。

- [ ] **步骤 3：实现 taskBus.ts**

创建 `src/utils/taskBus.ts`：

```ts
// src/utils/taskBus.ts — 任务事件总线
// 职责:定义事件契约,提供订阅/发布。不持有业务状态,不依赖 UI 与 Android API。
// 发布为同步调用:订阅者按注册顺序依次执行,任一订阅者抛出的异常会向上冒泡到发布方。

/** 单个任务结束时发布 */
export interface 任务结束事件 {
  类型: '任务结束'
  名: string                              // doTask 的 label
  状态: '成功' | '跳过' | '异常'
  原因?: string                           // 跳过/异常的原因;成功时无
  耗时: number                            // 秒,一位小数
  服?: string                             // 当前区服(多账号模式用于定位)
}

/** runDaily 开始时发布,订阅者据此清零 / 重置 UI */
export interface 本轮开始事件 {
  类型: '本轮开始'
}

/** runDaily 结束时发布(正常返回或异常中断都会发) */
export interface 整轮结束事件 {
  类型: '整轮结束'
  结果: '完成' | '已停止'
}

export type 任务事件 = 任务结束事件 | 本轮开始事件 | 整轮结束事件

var 监听器: Array<(e: 任务事件) => void> = []

/** 订阅事件,返回取消订阅函数(摘掉本次注册的这一个监听器) */
export function 订阅(fn: (e: 任务事件) => void): () => void {
  监听器.push(fn)
  return function (): void {
    var i = 监听器.indexOf(fn)
    if (i >= 0) 监听器.splice(i, 1)
  }
}

export function 发布(e: 任务事件): void {
  // 遍历快照:订阅者可能在回调里取消自己的订阅,直接遍历原数组会漏掉后续监听器
  var 快照 = 监听器.slice()
  for (var i = 0; i < 快照.length; i++) {
    快照[i](e)
  }
}
```

- [ ] **步骤 4：加 webpack entry**

修改 `webpack.config.js`，在 `entry` 对象里 `"详细日志诊断"` 那行之后加：

```js
    "任务通知诊断": path.resolve(__dirname, "./test/任务通知诊断.ts"),
```

- [ ] **步骤 5：运行测试验证通过**

运行：`npm run build` → 推送 `dist/任务通知诊断.js` + eval 执行（见「设备执行机制」）

预期日志：

```
===== 任务结果通知诊断 =====

--- 总线: 订阅与发布 ---
✅ 两个订阅者各收到一次, 实际 2
✅ 按注册顺序同步调用, 实际 [订阅A:本轮开始 | 订阅B:本轮开始]
✅ 取消订阅后不再收到, 实际 2

--- 总线: 取消只影响自己 ---
✅ 取消一个订阅者不影响另一个, 实际 留着=1 临时=0

--- 总线: 订阅者异常向上冒泡(契约行为) ---
✅ 订阅者抛错会冒泡到发布方(所以订阅者必须自行 try/catch)

================================
   结果: 5/5 通过
================================
```

- [ ] **步骤 6：Commit**

```bash
git add src/utils/taskBus.ts test/任务通知诊断.ts webpack.config.js
git commit -m "功能:新增任务事件总线与诊断脚本脚手架"
```

---

### 任务 2：taskReport 计数与明细

**文件：**
- 创建：`src/utils/taskReport.ts`
- 修改：`test/任务通知诊断.ts`（追加用例）

- [ ] **步骤 1：编写失败测试**

在 `test/任务通知诊断.ts` 顶部 import 区追加：

```ts
import { 本轮汇总, 重置本轮 } from '../src/utils/taskReport'
```

把 `summary()` 那一行替换成下面的新用例 + `summary()`：

```ts
// ---------- 计数与明细 ----------
function 成功(名: string): void {
  发布({ 类型: '任务结束', 名: 名, 状态: '成功', 耗时: 1 })
}
function 跳过(名: string, 原因?: string, 服?: string): void {
  发布({ 类型: '任务结束', 名: 名, 状态: '跳过', 原因: 原因, 耗时: 1, 服: 服 })
}
function 异常(名: string, 原因?: string): void {
  发布({ 类型: '任务结束', 名: 名, 状态: '异常', 原因: 原因, 耗时: 1 })
}

console.log('')
console.log('--- 计数: 三态混合 ---')
发布({ 类型: '本轮开始' })
成功('A'); 成功('B'); 跳过('C'); 异常('D')
var s = 本轮汇总()
assert(s.成功 === 2 && s.跳过 === 1 && s.异常 === 1,
  '三项计数 2/1/1, 实际 ' + s.成功 + '/' + s.跳过 + '/' + s.异常)
assert(s.条数 === 4, '条数 = 成功+跳过+异常 = 4, 实际 ' + s.条数)
assert(s.明细.length === 2, '明细只收跳过与异常, 实际 ' + s.明细.length)

console.log('')
console.log('--- 计数: 空轮 ---')
发布({ 类型: '本轮开始' })
var 空 = 本轮汇总()
assert(空.成功 === 0 && 空.跳过 === 0 && 空.异常 === 0 && 空.条数 === 0, '四项全 0')
assert(空.明细.length === 0, '明细为空')

console.log('')
console.log('--- 计数: 同名任务计多条 ---')
发布({ 类型: '本轮开始' })
成功('快速退出'); 成功('快速退出'); 成功('快速退出')
assert(本轮汇总().条数 === 3, '同名 3 次计 3 条(次数=条数语义), 实际 ' + 本轮汇总().条数)

console.log('')
console.log('--- 计数: 明细保留原始原因 ---')
发布({ 类型: '本轮开始' })
异常('无原因任务')
assert(本轮汇总().明细[0].原因 === '', '数据层存原始空串, 不预先兜底')

console.log('')
console.log('--- 计数: 重置本轮 ---')
发布({ 类型: '本轮开始' })
成功('X')
assert(本轮汇总().条数 === 1, '重置前 1 条')
重置本轮()
assert(本轮汇总().条数 === 0 && 本轮汇总().明细.length === 0, '重置本轮后清零')

console.log('')
console.log('--- 总线: 回调内取消自己不吞掉后续订阅者 ---')
// 覆盖 taskBus.ts 里 发布 遍历快照的行为:若直接遍历原数组,splice 位移会漏掉后一个监听器
var 甲收 = 0
var 乙收 = 0
var 取消甲 = 订阅(function (): void { 甲收++; 取消甲() })
var 取消乙 = 订阅(function (): void { 乙收++ })
发布({ 类型: '本轮开始' })
assert(甲收 === 1 && 乙收 === 1, '甲在回调里取消自己后,乙仍收到本次事件, 实际 甲=' + 甲收 + ' 乙=' + 乙收)
取消乙()

summary()
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npm run build`

预期：**FAIL** —— `Module not found: Can't resolve '../src/utils/taskReport'`。

- [ ] **步骤 3：实现计数部分**

创建 `src/utils/taskReport.ts`：

```ts
// src/utils/taskReport.ts — 本轮任务汇总(计数 / 明细 / 文案)
// 职责:订阅 taskBus,累加本轮结果;提供纯函数生成计数条文字与通知文案。
// 不依赖 UI 与 Android API,可在任何 JS 环境执行。
import { 订阅, 任务事件, 任务结束事件 } from './taskBus'

export interface 明细项 {
  状态: '跳过' | '异常'
  名: string
  原因: string          // 原始值,可能为空串;兜底文案由格式化函数负责
  服?: string
}

export interface 汇总结果 {
  成功: number
  跳过: number
  异常: number
  条数: number          // 恒等于 成功 + 跳过 + 异常
  明细: 明细项[]        // 跳过与异常,按发生顺序
}

function 空汇总(): 汇总结果 {
  return { 成功: 0, 跳过: 0, 异常: 0, 条数: 0, 明细: [] }
}

var 当前: 汇总结果 = 空汇总()

/** 取本轮汇总快照 */
export function 本轮汇总(): 汇总结果 {
  return 当前
}

/** 清空本轮计数与明细(由「本轮开始」事件自动触发,也可手工调用,仅诊断脚本会用) */
export function 重置本轮(): void {
  当前 = 空汇总()
}

function 记录(e: 任务结束事件): void {
  当前.条数++
  if (e.状态 === '成功') {
    当前.成功++
    return
  }
  if (e.状态 === '跳过') 当前.跳过++
  else 当前.异常++
  当前.明细.push({
    状态: e.状态,
    名: e.名,
    原因: e.原因 ? e.原因 : '',
    服: e.服,
  })
}

// 订阅在模块加载时完成;taskNotifier 在本模块之后加载,因此订阅顺序恒为
// taskReport 先于 taskNotifier —— notifier 读到的汇总总是已累加的最新值。
订阅(function (e: 任务事件): void {
  try {
    if (e.类型 === '本轮开始') {
      重置本轮()
      return
    }
    if (e.类型 === '任务结束') {
      记录(e)
      return
    }
  } catch (err: any) {
    console.log('[汇总] 处理事件失败: ' + (err && err.message ? err.message : err))
  }
})
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npm run build` → 推送 + eval 执行（见「设备执行机制」）

预期：`结果: 15/15 通过`（任务 1 的 5 条 + 本任务的 9 条 + 回调内取消 1 条），无 ❌。

- [ ] **步骤 5：Commit**

```bash
git add src/utils/taskReport.ts test/任务通知诊断.ts
git commit -m "功能:任务结果汇总计数与明细记录"
```

---

### 任务 3：taskReport 格式化文案

**文件：**
- 修改：`src/utils/taskReport.ts`（追加格式化函数）
- 修改：`test/任务通知诊断.ts`（追加用例）

> **本任务的两条断言是任务 2 遗留缺口的补丁，不能删**（任务 2 规格审查发现）：
> 1. 任务 2 的三态混合用例用的是**对称数据**（跳过×1、异常×1），把两者计反了也照样通过 → 本任务的「跳过与异常各自折叠」用例必须用**非对称数据**（跳过 6 / 异常 7），这样计反就会让 `跳过 N 项` 的标题对不上。
> 2. `明细项.状态` 字段在任务 2 没有运行期覆盖（改成恒为 `'跳过'` 仍是全绿）→ 本任务按状态分节渲染明细，正好把这个字段钉住。

- [ ] **步骤 1：编写失败测试**

把 `test/任务通知诊断.ts` 里任务 2 加的那行 import **改成一行**（合并，避免同模块两条 import）：

```ts
import {
  本轮汇总, 重置本轮,
  格式化计数条, 格式化通知标题, 格式化通知正文, 格式化通知明细, 格式化摘要,
} from '../src/utils/taskReport'
```

在 `summary()` 之前插入：

```ts
// ---------- 文案 ----------
console.log('')
console.log('--- 文案: 三态混合 ---')
发布({ 类型: '本轮开始' })
成功('A'); 成功('B'); 跳过('C'); 异常('D')
var t = 本轮汇总()
assert(格式化计数条(t) === '✅ 2  ⏭ 1  ❌ 1', '计数条文案, 实际 [' + 格式化计数条(t) + ']')
assert(格式化通知标题(t, '完成') === '日常完成 ✅2 ⏭1 ❌1', '完成标题, 实际 [' + 格式化通知标题(t, '完成') + ']')
assert(格式化通知标题(t, '已停止') === '日常已停止 ✅2 ⏭1 ❌1', '已停止标题, 实际 [' + 格式化通知标题(t, '已停止') + ']')
assert(格式化通知正文(t) === '成功 2 | 跳过 1 | 异常 1 | 合计 4', '正文文案, 实际 [' + 格式化通知正文(t) + ']')

console.log('')
console.log('--- 文案: 空轮 ---')
发布({ 类型: '本轮开始' })
var 空T = 本轮汇总()
assert(格式化通知明细(空T) === '', '明细为空串, 实际 [' + 格式化通知明细(空T) + ']')
assert(格式化通知标题(空T, '完成') === '日常完成 ✅0 ⏭0 ❌0', '空轮标题不抛异常')
assert(格式化通知正文(空T) === '成功 0 | 跳过 0 | 异常 0 | 合计 0', '空轮正文')

console.log('')
console.log('--- 文案: 原因缺失兜底 ---')
发布({ 类型: '本轮开始' })
异常('无原因任务')
var 缺失文本 = 格式化通知明细(本轮汇总())
assert(缺失文本.indexOf('— 未知原因') >= 0, '兜底为 未知原因, 实际 [' + 缺失文本 + ']')
assert(缺失文本.indexOf('undefined') < 0, '不出现 undefined, 实际 [' + 缺失文本 + ']')

console.log('')
console.log('--- 文案: 带服务器前缀 ---')
发布({ 类型: '本轮开始' })
跳过('军团 每日一刀', '未找到入口', 'S123')
assert(格式化通知明细(本轮汇总()).indexOf('· [S123] 军团 每日一刀 — 未找到入口') >= 0,
  '服务器前缀, 实际 [' + 格式化通知明细(本轮汇总()) + ']')

console.log('')
console.log('--- 边界: 恰好 5 条跳过 ---')
发布({ 类型: '本轮开始' })
for (var i5 = 1; i5 <= 5; i5++) 跳过('任务' + i5, '原因' + i5)
var 五条 = 格式化通知明细(本轮汇总())
assert(五条.indexOf('任务5') >= 0, '第 5 条列出')
assert(五条.indexOf('… 其余') < 0, '5 条不折叠, 实际 [' + 五条 + ']')

console.log('')
console.log('--- 边界: 6 条跳过 ---')
发布({ 类型: '本轮开始' })
for (var i6 = 1; i6 <= 6; i6++) 跳过('任务' + i6, '原因' + i6)
var 六条 = 格式化通知明细(本轮汇总())
assert(六条.indexOf('任务5') >= 0, '第 5 条仍列出')
assert(六条.indexOf('任务6') < 0, '第 6 条不列出')
assert(六条.indexOf('… 其余 1 项见运行日志') >= 0, '折叠尾行, 实际 [' + 六条 + ']')

console.log('')
console.log('--- 边界: 跳过与异常各自折叠 ---')
发布({ 类型: '本轮开始' })
for (var i7 = 1; i7 <= 6; i7++) 跳过('跳过' + i7)
for (var i8 = 1; i8 <= 7; i8++) 异常('异常' + i8)
var 双节 = 格式化通知明细(本轮汇总())
assert(双节.indexOf('跳过 6 项') >= 0, '跳过节标题带真实条数, 实际 [' + 双节 + ']')
assert(双节.indexOf('异常 7 项') >= 0, '异常节标题带真实条数')
assert(双节.indexOf('… 其余 1 项见运行日志') >= 0 && 双节.indexOf('… 其余 2 项见运行日志') >= 0,
  '两节各自折叠, 实际 [' + 双节 + ']')

console.log('')
console.log('--- 摘要: 整轮结束时的日志块 ---')
发布({ 类型: '本轮开始' })
成功('A'); 跳过('B', '入口未找到'); 异常('C', 'NavigationError: 示例')
var 完成摘要 = 格式化摘要(本轮汇总(), '完成')
assert(完成摘要.indexOf('日常任务 — 完成') >= 0, '完成摘要含标题, 实际 [' + 完成摘要 + ']')
assert(完成摘要.indexOf('成功: 1 | 跳过: 1 | 异常: 1 | 合计: 3') >= 0, '完成摘要含四项计数')
var 停止摘要 = 格式化摘要(本轮汇总(), '已停止')
assert(停止摘要.indexOf('日常任务 — 已停止') >= 0, '已停止摘要含对应标题, 实际 [' + 停止摘要 + ']')
assert(停止摘要.indexOf('日常任务 — 完成') < 0, '已停止摘要不含完成字样')
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npm run build`

预期：**FAIL** —— `TS2305: Module '"../src/utils/taskReport"' has no exported member '格式化计数条'`。

- [ ] **步骤 3：实现格式化函数**

在 `src/utils/taskReport.ts` 末尾追加：

```ts
/** 每节最多列出的明细条数,超出折叠为一行提示 */
var 每节上限 = 5

/** 计数条文字,如 "✅ 12  ⏭ 3  ❌ 2" */
export function 格式化计数条(汇总: 汇总结果): string {
  return '✅ ' + 汇总.成功 + '  ⏭ ' + 汇总.跳过 + '  ❌ ' + 汇总.异常
}

/** 通知标题,如 "日常完成 ✅12 ⏭3 ❌2" */
export function 格式化通知标题(汇总: 汇总结果, 结果: '完成' | '已停止'): string {
  var 前缀 = 结果 === '完成' ? '日常完成' : '日常已停止'
  return 前缀 + ' ✅' + 汇总.成功 + ' ⏭' + 汇总.跳过 + ' ❌' + 汇总.异常
}

/** 通知折叠态正文,如 "成功 12 | 跳过 3 | 异常 2 | 合计 17" */
export function 格式化通知正文(汇总: 汇总结果): string {
  return '成功 ' + 汇总.成功 + ' | 跳过 ' + 汇总.跳过 + ' | 异常 ' + 汇总.异常 + ' | 合计 ' + 汇总.条数
}

/** 通知展开态明细;无跳过与异常时返回空串(调用方据此决定是否传 bigContent) */
export function 格式化通知明细(汇总: 汇总结果): string {
  var 跳过项 = 取明细(汇总, '跳过')
  var 异常项 = 取明细(汇总, '异常')
  if (跳过项.length === 0 && 异常项.length === 0) return ''
  var 行: string[] = []
  if (跳过项.length > 0) 追加节(行, '跳过', 跳过项)
  if (异常项.length > 0) 追加节(行, '异常', 异常项)
  return 行.join('\n')
}

function 取明细(汇总: 汇总结果, 状态: '跳过' | '异常'): 明细项[] {
  var 结果: 明细项[] = []
  for (var i = 0; i < 汇总.明细.length; i++) {
    if (汇总.明细[i].状态 === 状态) 结果.push(汇总.明细[i])
  }
  return 结果
}

function 追加节(行: string[], 标题: string, 项: 明细项[]): void {
  行.push(标题 + ' ' + 项.length + ' 项')
  var 显示数 = 项.length > 每节上限 ? 每节上限 : 项.length
  for (var i = 0; i < 显示数; i++) {
    行.push('· ' + 格式化明细行(项[i]))
  }
  if (项.length > 每节上限) {
    行.push('… 其余 ' + (项.length - 每节上限) + ' 项见运行日志')
  }
}

function 格式化明细行(项: 明细项): string {
  var 服前缀 = 项.服 ? '[' + 项.服 + '] ' : ''
  var 原因 = 项.原因 ? 项.原因 : '未知原因'
  return 服前缀 + 项.名 + ' — ' + 原因
}

/** 整轮结束时的 console 摘要块(替代原 daily.ts 里那 6 行手写摘要) */
export function 格式化摘要(汇总: 汇总结果, 结果: '完成' | '已停止'): string {
  var 行: string[] = []
  行.push('')
  行.push('================================')
  行.push(结果 === '完成' ? '   日常任务 — 完成' : '   日常任务 — 已停止')
  行.push('   成功: ' + 汇总.成功 + ' | 跳过: ' + 汇总.跳过 + ' | 异常: ' + 汇总.异常 + ' | 合计: ' + 汇总.条数)
  行.push('================================')
  return 行.join('\n')
}
```

**同时要给订阅回调补上「整轮结束」分支**——这是规格 §5 要求的摘要输出路径，与通知开关**无关**（「执行结果通知」默认关闭，若摘要只在通知里输出，默认配置下跑完一轮日志里将没有任何汇总）。

a) 把 `src/utils/taskReport.ts` 第 4 行的 import 改为（加 `整轮结束事件`）：

```ts
import { 订阅, 任务事件, 任务结束事件, 整轮结束事件 } from './taskBus'
```

b) 把 `订阅(...)` 回调体补上第三个分支，并加一个处理函数：

```ts
订阅(function (e: 任务事件): void {
  try {
    if (e.类型 === '本轮开始') {
      重置本轮()
      return
    }
    if (e.类型 === '任务结束') {
      记录(e)
      return
    }
    if (e.类型 === '整轮结束') {
      打印摘要(e)
    }
  } catch (err: any) {
    console.log('[汇总] 处理事件失败: ' + (err && err.message ? err.message : err))
  }
})

/** 整轮结束:把摘要写进日志(不依赖 taskNotifier,通知开关关闭时也要留档) */
function 打印摘要(e: 整轮结束事件): void {
  console.log(格式化摘要(本轮汇总(), e.结果))
}
```

**同时按任务 2 代码质量审查的结论改四处注释**（都是纯注释改动，不改行为）：

1. `本轮汇总()` 的注释改为——原文写「取本轮汇总快照」，但返回的是活引用，措辞会误导：

```ts
/** 本轮汇总对象本身(活引用,非副本)。两条使用约束:
 *  1) 只读:勿改字段、勿用 明细.sort() 之类原地方法,会污染后续计数与明细顺序;
 *  2) 勿缓存:重置本轮 与「本轮开始」会整体换新对象,早先拿到的引用会停在旧数据上不再更新。 */
export function 本轮汇总(): 汇总结果 {
```

2. 订阅回调上方的顺序保证注释补前提（顺序是靠 `taskNotifier` import 本模块换来的，不是天然成立）：

```ts
// 订阅在模块加载时完成;顺序保证:taskNotifier 会 import 本模块,ES 模块先求值依赖,
// 因此订阅顺序恒为 taskReport 先于 taskNotifier —— notifier 读到的汇总总是已累加的最新值。
// 不保存取消句柄:本模块的订阅与脚本同生命周期,没有取消场景(规格「已知限制 4」)。
```

3. 文件第 2 行职责注释把副作用提到句首：

```ts
// 职责:模块加载时即订阅 taskBus(本模块唯一的副作用,无取消入口),累加本轮结果;
//       提供纯函数生成计数条文字、通知文案与日志摘要。不依赖 UI 与 Android API。
```

4. `明细项.原因` 的注释补一句归一化来源：

```ts
  原因: string          // 原始值;事件未带原因时已在上游归一为空串,兜底文案由格式化函数负责
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npm run build` → 推送 + eval 执行（见「设备执行机制」）

预期：`结果: 37/37 通过`（任务 2 后的 15 条 + 本任务 18 条文案断言 + 4 条摘要断言），无 ❌。

**特别确认**：日志里应当出现一整块摘要（由 `打印摘要` 打印，来自上面那条 `发布({ 类型: '整轮结束', ... })` 断言段触发时**不会**打印，因为断言段只是调 `格式化摘要()` 纯函数；摘要的实际打印要等任务 5 的渲染冒烟段发「整轮结束」事件时才看得到）。

> 该断言段里对 `格式化摘要()` 的调用是纯函数调用，不会触发 `订阅` 回调，因此不会污染日志。

- [ ] **步骤 5：Commit**

```bash
git add src/utils/taskReport.ts test/任务通知诊断.ts
git commit -m "功能:任务结果计数条与通知文案格式化"
```

---

### 任务 4：计数条悬浮窗

渲染部分依赖 floaty，无法在日志里断言，靠人眼确认。

**文件：**
- 创建：`layout/status.xml`
- 创建：`src/utils/taskNotifier.ts`
- 修改：`test/任务通知诊断.ts`（追加冒烟段）

> **本任务要顺带补任务 3 遗留的两条断言**（任务 3 的变异自查发现，两条都是零红灯缺口——改错了也测不出来）：
>
> 1. **「整轮结束」分支无覆盖**：诊断脚本里的文案断言只调纯函数 `格式化摘要()`，从不 `发布` 该事件，所以订阅回调里那个分支删掉也全绿。这正是任务 2 漏掉整个分支却没人发现的原因。补法是临时接管 `console.log` 捕获输出：
>
> ```ts
> console.log('')
> console.log('--- 摘要: 整轮结束事件真的触发了打印 ---')
> var 捕获: string[] = []
> var 原摘要log = console.log
> console.log = function (): void { 捕获.push(Array.prototype.slice.call(arguments).join(' ')) }
> 发布({ 类型: '本轮开始' })
> 成功('A'); 跳过('B', '入口未找到')
> 发布({ 类型: '整轮结束', 结果: '完成' })
> console.log = 原摘要log
> var 捕获文本 = 捕获.join('\n')
> assert(捕获文本.indexOf('日常任务 — 完成') >= 0, '整轮结束事件触发摘要打印, 实际 [' + 捕获文本 + ']')
> assert(捕获文本.indexOf('成功: 1 | 跳过: 1 | 异常: 0 | 合计: 2') >= 0, '摘要内容与汇总一致')
> ```
>
> 2. **明细两节的顺序无覆盖**：把 `格式化通知明细` 里「先跳过后异常」对调，37 条断言全绿。在任务 3 的「跳过与异常各自折叠」用例末尾补一条：
>
> ```ts
> assert(双节.indexOf('跳过 6 项') < 双节.indexOf('异常 7 项'), '跳过节排在异常节之前, 实际 [' + 双节 + ']')
> ```

- [ ] **步骤 1：创建布局**

创建 `layout/status.xml`（属性写法照抄 `layout/small.xml`，`<text>` 对应 `JsTextView`）：

```xml
<text id="计数" w="150" h="30" alpha="0.8" padding="0" margin="0">✅ 0  ⏭ 0  ❌ 0</text>
```

> 构建期 `plugin/adaptive/index.js:32-47` 会把 `'layoutFile:../layout/status.xml'` 这个字符串字面量替换成文件内容内联进 `dist/*.js`，**不需要任何额外配置**。

- [ ] **步骤 2：编写冒烟测试（会失败）**

在 `test/任务通知诊断.ts` import 区追加：

```ts
import { 初始化通知 } from '../src/utils/taskNotifier'
```

在 `summary()` 之前插入。计数条的文字**可以程序化断言**（exec 上下文里 `floaty` 的文字能读回，已实测），只有排版可读性需要人眼：

```ts
// ---------- 渲染冒烟: 计数条 ----------
console.log('')
console.log('--- 渲染冒烟: 计数条 ---')
初始化通知(true)
发布({ 类型: '本轮开始' })
sleep(600)
成功('冒烟-成功A'); sleep(700)
成功('冒烟-成功B'); sleep(700)
跳过('冒烟-跳过任务', '入口未找到'); sleep(700)
异常('冒烟-异常任务', 'NavigationError: 示例'); sleep(700)

// 通过 floaty 句柄读回真实文字,和文案函数比
var 读回的计数文字 = (global as any).__计数条文字
assert(读回的计数文字 === '✅ 2  ⏭ 1  ❌ 1',
  '计数条文字与汇总一致, 实际 [' + 读回的计数文字 + ']')
console.log('  仍需人眼确认: 计数条在小球正下方、可拖动、结束时仍留在屏上、亮色背景下看得清')
```

> 为了让脚本能读到窗口文字，`taskNotifier.ts` 在 `刷新计数条` 后额外写一行 `(global as any).__计数条文字 = 文字`。这是给诊断用的观测点，正常运行路径不受影响——**实现时必须加上，否则上面的断言读不到值**。

- [ ] **步骤 3：运行验证失败**

运行：`npm run build`

预期：**FAIL** —— `Can't resolve '../src/utils/taskNotifier'`。

- [ ] **步骤 4：实现 taskNotifier 的计数条**

创建 `src/utils/taskNotifier.ts`：

```ts
// src/utils/taskNotifier.ts — 结果通知的渲染层
// 职责:订阅 taskBus,把汇总渲染到右上角计数条悬浮窗,并在整轮结束时发系统通知。
// 开关关闭时全部短路:不建窗、不发通知,也不影响 taskReport 继续计数。
import { FloatWindow } from '../../component/FloatWindow'
import { 订阅, 任务事件 } from './taskBus'
import { 本轮汇总, 汇总结果, 格式化计数条 } from './taskReport'

/** 计数条悬浮窗(单例)。独立于 SmallWindows.ts 的 smallWindow:
 *  后者的 close() 里是 threads.shutDownAll(),共用会误杀任务线程。 */
class 计数条窗口 extends FloatWindow<{ 计数: View & JsTextView }> {
  private 已显示 = false
  constructor() {
    super('layoutFile:../layout/status.xml', false)
    this.window.setPosition(device.width - 150, 60)
    this.draggable()
  }
  /** 写文字并确保可见(doTask 跑在子线程,必须走 ui.run) */
  显示(文字: string): void {
    ui.run(() => {
      this.window.计数.setText(文字)
      if (!this.已显示) {
        this.已显示 = true
        this.mView.setVisibility(0)
      }
    })
  }
}

var 窗口: 计数条窗口 | null = null
var 启用 = false
var 已订阅 = false

/** 订阅只注册一次;每次调用按传入值刷新启用状态 */
export function 初始化通知(开启: boolean): void {
  启用 = 开启
  if (已订阅) return
  已订阅 = true
  订阅(处理事件)
}

function 处理事件(e: 任务事件): void {
  try {
    if (!启用) return
    if (e.类型 === '本轮开始') {
      刷新计数条(本轮汇总())
      return
    }
    if (e.类型 === '任务结束') {
      刷新计数条(本轮汇总())
      return
    }
    if (e.类型 === '整轮结束') {
      刷新计数条(本轮汇总())
    }
  } catch (err: any) {
    // 订阅者异常会冒泡进 doTask 被误判成任务失败,这里必须兜住
    console.log('[通知] 处理事件失败: ' + (err && err.message ? err.message : err))
  }
}

function 刷新计数条(汇总: 汇总结果): void {
  var 文字 = 格式化计数条(汇总)
  if (!窗口) 窗口 = new 计数条窗口()
  窗口.显示(文字)
  // 诊断观测点:把当前文字暴露到 global,供 test/任务通知诊断.ts 断言
  ;(global as any).__计数条文字 = 文字
}
```

> **时序说明**：`本轮开始` 分支读的是 `本轮汇总()`，此刻 `taskReport` 已经把它清零——因为 `taskNotifier` import 了 `taskReport`，ES 模块保证 `taskReport` 的模块体（含它的订阅注册）先于 `taskNotifier` 求值，所以订阅顺序恒为 report → notifier。

- [ ] **步骤 5：运行验证通过**

运行：`npm run build` → 推送 + eval 执行（见「设备执行机制」）

预期：`38/38 通过`（37 + 新增的计数条断言），并看到：

```
--- 渲染冒烟: 计数条 ---
✅ 计数条文字与汇总一致, 实际 [✅ 2  ⏭ 1  ❌ 1]
  仍需人眼确认: 计数条在小球正下方、可拖动、结束时仍留在屏上、亮色背景下看得清
```

**人眼确认三点**：位置是否在小球正下方且能拖动；脚本跑完后是否仍在屏上；亮色游戏界面上文字是否看得清。

若文字在亮色游戏界面上看不清，或在三位数时被截断，在此记录实测结论、调整 `layout/status.xml` 的 `w`/`alpha`（属性名以设备实测为准，不要凭空添加）。

- [ ] **步骤 6：Commit**

```bash
git add layout/status.xml src/utils/taskNotifier.ts test/任务通知诊断.ts
git commit -m "功能:右上角常驻任务计数条悬浮窗"
```

---

### 任务 5：系统通知发送

**文件：**
- 修改：`src/utils/taskNotifier.ts`
- 修改：`test/任务通知诊断.ts`

> **本任务的通道与原计划不同**：原设计用 AutoJs6 的 `notice()`，但设备实测该 API 不存在（AutoX.js v6.6.7）。改用 Android 原生 `NotificationManager`，已实测发送成功。规格 §4 已同步修订，实现以本节为准。

> **本任务要顺带补任务 4 遗留的一条零红灯断言**（任务 4 变异自查发现）：`taskNotifier.处理事件` 里的 `if (!启用) return` 守卫删掉后 41/41 全绿——因为诊断脚本里 `启用` 恒为 true。补法是先关掉开关再发事件，断言计数条文字**保持不动**。加在渲染冒烟段之前：
>
> ```ts
> console.log('')
> console.log('--- 开关关闭时不刷计数条 ---')
> 初始化通知(false)
> var 关前 = (global as any).__计数条文字
> 发布({ 类型: '任务结束', 名: '关了开关也不该动', 状态: '成功', 耗时: 1 })
> var 关后 = (global as any).__计数条文字
> assert(关前 === 关后, '开关关闭时计数条不刷新, 实际 [' + 关前 + '] → [' + 关后 + ']')
> 初始化通知(true)
> ```
>
> **另一处「零红灯」经判定不是缺口，不补断言**：任务 4 的规格审查发现，注释掉 `taskNotifier` 的「整轮结束 → 刷新计数条」分支也是 41/41 全绿。原因是那条刷新**本就是冗余的**——最后一个「任务结束」事件已经把计数条刷成最终值，再刷一次结果相同，所以任何断言都无法区分。该分支真正的载荷是**下面要加的通知发送**，由本任务的通知断言覆盖。刷新那一行保留：一行代价，换来"计数条内容恒等于最近一次汇总"的构造性保证。
>
> **另外本任务要顺手修任务 4 质量审查报的两条真问题**（都在 `taskNotifier.ts`，趁改这个文件一起做）：
>
> **① 建窗在 `ui.run` 之外。** 原实现的注释写着"doTask 跑在子线程，必须走 ui.run"，但 `new 计数条窗口()`（含 `setPosition` / `draggable`）在 `ui.run` 外面。生产上 `main.ts:190` 是 `threads.start` 子线程，首次事件就会在子线程建窗——而规格实测的结论只是"**在 `ui.run` 回调里**建窗可用"，这条路没被验证过。修法是把 `ui.run` 上提到 `刷新计数条`，窗口类内的 `ui.run` 随之取消：
>
> ```ts
> function 刷新计数条(汇总: 汇总结果): void {
>   var 文字 = 格式化计数条(汇总)
>   // 建窗与写文字都是 UI 操作:doTask 跑在子线程,必须在 ui.run 里做
>   ui.run(() => {
>     if (!窗口) 窗口 = new 计数条窗口()
>     窗口.显示(文字)
>   })
>   // 诊断观测点:把当前文字暴露到 global,供 test/任务通知诊断.ts 断言
>   ;(global as any).__计数条文字 = 文字
> }
> ```
>
> 同时删掉 `计数条窗口` 的 `私有 已显示` 标志（`setVisibility(0)` 在 flag 未变时是空操作，加标志反而引入"新增隐藏路径必须复位它"的隐含约束），并给窗口类加一个 `隐藏()`。
>
> **② 关开关不撤窗。** 关掉「执行结果通知」后，上一轮留下的计数条会带着旧数字僵在屏上。`初始化通知(false)` 要隐藏已有窗口：
>
> ```ts
> export function 初始化通知(开启: boolean): void {
>   启用 = 开启
>   if (!开启 && 窗口) {
>     // 关掉开关时撤掉上一轮留下的计数条,否则它会带着旧数字僵在屏上
>     var 旧 = 窗口
>     ui.run(() => { 旧.隐藏() })
>   }
>   if (已订阅) return
>   已订阅 = true
>   订阅(处理事件)
> }
> ```
>
> 文件头注释同步改成「开关关闭时**不建窗、隐藏已有窗**、不发通知」。

- [ ] **步骤 1：编写测试（会失败）**

在 `test/任务通知诊断.ts` 的渲染冒烟段末尾（`console.log('  请确认: 计数条...')` 之后）追加：

```ts
console.log('')
console.log('--- 渲染冒烟: 通知 ---')
发布({ 类型: '整轮结束', 结果: '完成' })
sleep(1500)
var nm = context.getSystemService(android.content.Context.NOTIFICATION_SERVICE)
var 活跃 = nm.getActiveNotifications()
var 找到 = null
for (var ia = 0; ia < 活跃.length; ia++) {
  if (String(活跃[ia].getId()) === '1919') 找到 = 活跃[ia]
}
assert(!!找到, '通知已发出(id=1919), 活跃数 ' + 活跃.length)
if (找到) {
  var 通知对象 = 找到.getNotification()
  var 标题文本 = String(通知对象.extras.getString(android.app.Notification.EXTRA_TITLE))
  assert(标题文本 === '日常完成 ✅2 ⏭1 ❌1', '通知标题正确, 实际 [' + 标题文本 + ']')
  var 展开文本 = String(通知对象.extras.getString(android.app.Notification.EXTRA_BIG_TEXT))
  assert(展开文本.indexOf('跳过 1 项') >= 0 && 展开文本.indexOf('冒烟-跳过任务') >= 0,
    '展开态含跳过明细, 实际 [' + 展开文本 + ']')
  nm.cancel(1919)
  console.log('  已取消测试通知(id=1919)')
}
console.log('  仍需人眼确认: 通知下拉展开后的排版是否清晰')
```

- [ ] **步骤 2：运行验证失败**

运行：`npm run build` → 推送 → eval 执行

预期：前面断言全绿，但 **❌ 通知已发出(id=1919)**（`整轮结束` 分支还没实现发送）。

- [ ] **步骤 3：实现通知发送**

修改 `src/utils/taskNotifier.ts`：

a) import 行补上三个格式化函数：

```ts
import { 订阅, 任务事件 } from './taskBus'
import {
  本轮汇总, 汇总结果,
  格式化计数条, 格式化通知标题, 格式化通知正文, 格式化通知明细,
} from './taskReport'
```

b) 在 `var 已订阅 = false` 之后加常量：

```ts
/** 通知 id 固定,同一设备上本轮覆盖上一轮,通知栏不堆积 */
var 通知ID = 1919
/** 通知渠道 id,Android 8+ 必须 */
var 渠道ID = 'xjskp_result'
```

c) `处理事件` 的 `整轮结束` 分支改为：

```ts
    if (e.类型 === '整轮结束') {
      刷新计数条(本轮汇总())
      发送通知(e.结果)
    }
```

d) 文件末尾追加：

```ts
/** Android 8(API 26)起通知必须归属渠道;低版本没有渠道概念,直接跳过 */
function 确保渠道(nm: any): void {
  if (device.sdkInt < 26) return
  var 渠道 = new android.app.NotificationChannel(渠道ID, '脚本执行结果',
    android.app.NotificationManager.IMPORTANCE_DEFAULT)
  // 幂等:重复创建只更新名称与描述
  nm.createNotificationChannel(渠道)
}

function 发送通知(结果: '完成' | '已停止'): void {
  var 汇总 = 本轮汇总()
  var 标题 = 格式化通知标题(汇总, 结果)
  var 正文 = 格式化通知正文(汇总)
  var 明细 = 格式化通知明细(汇总)
  try {
    // Android 原生 API: context.getSystemService(NOTIFICATION_SERVICE) / NotificationChannel
    //                   / Notification.Builder / NotificationManager.notify
    // 设备实测: API 33, areNotificationsEnabled() 为 true, 无需运行时申请权限
    var nm = context.getSystemService(android.content.Context.NOTIFICATION_SERVICE)
    确保渠道(nm)
    // API 26+ 的 Builder 构造函数必须带渠道 id;低版本用单参构造
    var 构造器 = device.sdkInt >= 26
      ? new android.app.Notification.Builder(context, 渠道ID)
      : new android.app.Notification.Builder(context)
    构造器.setContentTitle(标题)
    构造器.setContentText(正文)
    if (明细) {
      // 展开态长文本(原设计的 bigContent 等价物)
      构造器.setStyle(new android.app.Notification.BigTextStyle().bigText(明细))
    }
    // setSmallIcon 必须调用,缺了小图标 notify() 不报错但通知不显示
    构造器.setSmallIcon(android.R.drawable.ic_dialog_info)
    构造器.setAutoCancel(true)
    nm.notify(通知ID, 构造器.build())
    console.log('[通知] 已发送: ' + 标题)
  } catch (e: any) {
    console.log('[通知] 发送失败: ' + (e && e.message ? e.message : e))
    // 降级: 结果仍进日志与详细日志文件
    console.log('[通知] 降级输出: ' + 标题 + ' | ' + 正文)
    toast('通知发送失败，结果见运行日志')
  }
}
```

- [ ] **步骤 4：运行验证通过**

运行：`npm run build` → 推送 → eval 执行

预期：`38/38` → 变 `41/41 通过`（新增 3 条通知断言），日志含：

```
--- 渲染冒烟: 通知 ---
[通知] 已发送: 日常完成 ✅2 ⏭1 ❌1
✅ 通知已发出(id=1919), 活跃数 3
✅ 通知标题正确, 实际 [日常完成 ✅2 ⏭1 ❌1]
✅ 展开态含跳过明细, 实际 [跳过 1 项
· 冒烟-跳过任务 — 入口未找到
异常 1 项
· 冒烟-异常任务 — NavigationError: 示例]
  已取消测试通知(id=1919)
```

**再做一次覆盖验证**：把诊断脚本连续 eval 两次，第二次执行后 `getActiveNotifications()` 里 id 1919 **仍只出现一次**（同 id 覆盖而非堆积）。

**人眼确认**（真机上做一次）：跑一次诊断脚本，在下拉通知栏里看到这条通知，点一下能划掉（`setAutoCancel` 生效），展开后排版可读。

- [ ] **步骤 5：Commit**

```bash
git add src/utils/taskNotifier.ts test/任务通知诊断.ts
git commit -m "功能:整轮结束发送结果通知栏通知"
```

---

### 任务 6：daily.ts 埋点改造

把四个模块级计数器换成事件发布，收敛三处裸计数，修掉观影签到的重复计数。

**文件：**
- 修改：`src/utils/taskNotifier.ts`（1 行，任务 5 质量审查遗留）
- 修改：`src/model/daily.ts`（import 区、`:62-65`、`doTask`、33 处 nav、27 个动作函数注解、观影签到条目、`批量执行活动` 三处、`runDaily` 开头清零）

- [ ] **步骤 0：先修 taskNotifier 的一行（独立提交）**

`src/utils/taskNotifier.ts` 里诊断观测点那行是 `;(global as any).__计数条文字 = 文字`。第 13 行已经有 `declare var global: any`，所以 `as any` 是多余的；而正因为加了括号，语句以 `(` 开头、紧跟在 `ui.run(...)` 之后会被 ASI 解析成调用链（`ui.run(...)(global as any).__计数条文字 = 文字`），**那个前导分号是必需的补丁**——将来谁"顺手清理"它就会踩运行时 TypeError。

改成：

```ts
  // 诊断观测点:把当前文字暴露到 global,供 test/任务通知诊断.ts 断言
  global.__计数条文字 = 文字
```

去掉 `as any` 与前置分号，两者互为因果，一起消失。改完 `npm run build` + 推设备跑一遍，确认仍 **45/45**，然后单独提交：

```bash
git add src/utils/taskNotifier.ts
git commit -m "重构:诊断观测点去掉多余的 as any 与前导分号"
```

- [ ] **步骤 1：加 import**

在 `src/model/daily.ts` 的 import 区末尾（`import { 道具购买Page } from "../pages/道具购买"` 之后）加：

```ts
import { 发布 } from '../utils/taskBus'
import { 初始化通知 } from '../utils/taskNotifier'
```

> `初始化通知` 在本任务只 import 不使用，任务 7 才会调用——先加进来是为了让构建能一次性暴露循环依赖问题（若有）。TS 允许未使用的 import。

- [ ] **步骤 2：删四个计数器**

把：

```ts
// 页面实例统一由 pages.ts 注册表创建与注册(见顶部 import);识别优先级见 pages.ts 内分组注释
var totalTasks = 0
var successTasks = 0
var skipTasks = 0
var failTasks = 0
export var currentServer: string | null = null
```

替换为：

```ts
// 页面实例统一由 pages.ts 注册表创建与注册(见顶部 import);识别优先级见 pages.ts 内分组注释
// 本轮计数与明细统一由 utils/taskReport.ts 维护(订阅 taskBus 事件),此处不再持有计数器
export var currentServer: string | null = null
```

- [ ] **步骤 3：改造 doTask**

把整个 `doTask` 函数（原 `:116-141`）替换为：

```ts
/** 任务动作返回值:true=成功;false=跳过(原因记为"未完成");对象=按 ok 判定并携带原因 */
type 动作返回 = boolean | { ok: boolean; 原因?: string }

/** 执行一个日常任务。配置.计数 传 false 时不发布事件(用于只串接子任务、自身无独立动作的父任务) */
function doTask(label: string, action: () => 动作返回, 配置?: { 计数?: boolean }): boolean {
  var 要计数 = !配置 || 配置.计数 !== false
  var 服 = currentServer ? currentServer : undefined
  console.log('[日常] 开始: ' + label)
  var start = Date.now()
  try {
    var r = action()
    var ok = typeof r === 'boolean' ? r : r.ok
    var 动作原因 = typeof r === 'boolean' ? '' : (r.原因 ? r.原因 : '')
    var elapsed = Number(((Date.now() - start) / 1000).toFixed(1))
    if (ok) {
      console.log('[日常] ✅ ' + label + ' (' + elapsed + 's)')
      if (要计数) 发布({ 类型: '任务结束', 名: label, 状态: '成功', 耗时: elapsed, 服: 服 })
      return true
    }
    var serverTag = currentServer ? ' [' + currentServer + ']' : ''
    var 跳过原因 = 动作原因 ? 动作原因 : '未完成'
    console.log(serverTag + '[日常] ⏭️ ' + label + ' — 跳过[' + 跳过原因 + '] (' + elapsed + 's)')
    if (要计数) 发布({ 类型: '任务结束', 名: label, 状态: '跳过', 原因: 跳过原因, 耗时: elapsed, 服: 服 })
    return false
  } catch (e: any) {
    // 手动停止时立即终止
    if (isStopException(e)) throw e
    var elapsed = Number(((Date.now() - start) / 1000).toFixed(1))
    var serverTag = currentServer ? ' [' + currentServer + ']' : ''
    var 原因 = e && e.message ? String(e.message) : String(e)
    console.log(serverTag + '[日常] ❌ ' + label + ' — ' + 原因 + ' (' + elapsed + 's)')
    if (要计数) 发布({ 类型: '任务结束', 名: label, 状态: '异常', 原因: 原因, 耗时: elapsed, 服: 服 })
    return false
  }
}
```

**同时把全部 `if (!nav(X)) return false` 改成 `if (!nav(X)) return { ok: false, 原因: '导航失败' }`。** 这条规则让"进不去页面"和"进去了没事可做"在通知里可区分：

```bash
grep -c "if (!nav(" src/model/daily.ts          # 得 36 = 33 个单行 + 3 个多行块
grep -c ")) return false" src/model/daily.ts    # 得 35 = 33 个 nav + 2 个 ticketAction
```

**实际要改的是 33 处**（只改 nav）。那 2 处 `if (!ticketAction()) return false`（买票动作）**保持原样**——它们的失败会记成「未完成」，"军团商店没券"与"进去了没事可做"暂时同一文案，更细的原因留给将来在具体调用点用 `{ ok: false, 原因: '...' }` 显式提供。

逐条替换（同名的用 `replace_all`，例如 `if (!nav(军团)) return false` 出现两次）。改完再跑一次：

```bash
grep -n "if (!nav(.*)) return false" src/model/daily.ts   # 应为空
grep -c "原因: '导航失败'" src/model/daily.ts             # 应等于 33
```

**本步骤还要放宽 27 个内层动作函数的返回注解**：`执行: function (): boolean {` 这类**带显式注解**的函数表达式，一旦返回 `{ ok: false, ... }` 就会报 `TS2322`。把这些改成 `function (): 动作返回 {`。不返回对象的 7 个（`闪退`、`快速退出`、`先锋宝藏`、`碧海凉夏免费`、`观影签到` 父子、`影映观礼`）保持 `: boolean`。靠 `npm run build` 的报错驱动，逐个改到通过为止。

> 注意 `批量执行活动` 里那三处 `if (!nav(...)) {` 是**多行块**（不是 `return false`），不在本步骤范围，由步骤 6 单独处理。

- [ ] **步骤 4：观影签到去重**

三处改动，缺一不可。

a) `interface 活动目标`（`src/model/daily.ts:351-356`）加一个可选字段：

```ts
interface 活动目标 {
  名: string
  页: BasePage
  入口图: string
  执行: () => 动作返回   // 步骤 3 新定义的联合类型
  计数?: boolean        // false = 本条目只是串接子任务,自身不计数
}
```

b) 观影签到那条 `列表.push({...})`（`:408`）加 `计数: false`：

```ts
      列表.push({
        名: '观影签到', 页: 观影签到Page, 入口图: IMG.战斗观影签到,
        // 本条目只是串接两个子任务(签到、便利店),自身无独立动作,故不计数
        计数: false,
        执行: function (): boolean {
```

该 `执行` 函数体保持原样（`return ok && ok2` / `return ok` 不动）——子任务各自已发过事件，父层只负责串接。

c) `批量执行活动` 里的 `doTask(目标.名, 目标.执行)`（`:471`）改为：

```ts
      // 目标.执行 可能自带子任务(如观影签到串接签到+便利店),由目标自行声明是否计数
      doTask(目标.名, 目标.执行, 目标.计数 === false ? { 计数: false } : undefined)
```

- [ ] **步骤 5：收敛三处裸计数**

a) `批量执行活动` 开头（原 `:435-440`）：加计时并替换 `failTasks++`

```ts
function 批量执行活动(): void {
  var 开始 = Date.now()
  if (!nav(战斗)) {
    console.log('[日常] ❌ 战斗页活动批量 — 无法进入战斗页')
    发布({
      类型: '任务结束', 名: '战斗页活动批量', 状态: '异常', 原因: '无法进入战斗页',
      耗时: Number(((Date.now() - 开始) / 1000).toFixed(1)), 服: currentServer ? currentServer : undefined,
    })
    return
  }
```

b) 目标入口点击失败（原 `:465-470`）：

```ts
      var 目标开始 = Date.now()
      if (!nav(目标.页.constructor as any)) {
        console.log('[日常] ❌ ' + 目标.名 + ' — 入口点击后未进入页面')
        发布({
          类型: '任务结束', 名: 目标.名, 状态: '异常', 原因: '入口点击后未进入页面',
          耗时: Number(((Date.now() - 目标开始) / 1000).toFixed(1)),
          服: currentServer ? currentServer : undefined,
        })
        nav(战斗)
        continue
      }
```

c) 无法返回战斗页（原 `:472-476`）：

```ts
      if (!nav(战斗)) {
        console.log('[日常] ❌ 战斗页活动批量 — 无法返回战斗页')
        发布({
          类型: '任务结束', 名: '战斗页活动批量', 状态: '异常', 原因: '无法返回战斗页',
          耗时: Number(((Date.now() - 开始) / 1000).toFixed(1)), 服: currentServer ? currentServer : undefined,
        })
        return
      }
```

- [ ] **步骤 6：验证 —— 构建通过且无残留引用**

运行：

```bash
npm run build
grep -n "totalTasks\|successTasks\|skipTasks\|failTasks" src/model/daily.ts
```

预期：构建无报错；`grep` **无任何输出**（说明四个计数器与全部 `failTasks++` 已清干净）。若 grep 有输出，说明漏改，回到步骤 2-5。

再运行一次诊断脚本，`npm run build` → 推送 + eval 执行，预期 `41/41 通过`（本任务不改变诊断脚本的断言，但它证明改造没有破坏模块加载）。

- [ ] **步骤 7：Commit**

```bash
git add src/model/daily.ts
git commit -m "功能:日常任务埋点接入事件总线,收敛裸计数并修复观影签到重复计数"
```

---

### 任务 7：runDaily 生命周期与摘要迁移

**文件：**
- 修改：`layout/layout.xml:104-105`（删占位注释）
- 修改：`src/model/daily.ts`（`runDaily`）

- [ ] **步骤 1：删掉开关的占位注释**

把 `layout/layout.xml` 里的：

```xml
      <pref-switch id="执行完息屏" text="执行完息屏" layoutDirection="rtl" />
      <!-- 占位:功能待实现,暂无代码读取该开关 -->
      <pref-switch id="执行结果通知" text="执行结果通知" layoutDirection="rtl" />
```

替换为：

```xml
      <pref-switch id="执行完息屏" text="执行完息屏" layoutDirection="rtl" />
      <pref-switch id="执行结果通知" text="执行结果通知" layoutDirection="rtl" />
```

开关本身（id、文案、默认关闭）保持不变，只删注释。

- [ ] **步骤 2：整体替换 runDaily**

把整个 `runDaily` 函数（原 `:487-524`）替换为：

```ts
export function runDaily(): void {
  // 开关在此读一次;放 runDaily 而非 start():start 是四个入口共用的,放那里会让兑换码/探索也建计数条
  初始化通知(isDailyEnabled('执行结果通知'))
  发布({ 类型: '本轮开始' })

  console.log('')
  console.log('================================')
  console.log('   日常任务 — 开始')
  console.log('================================')
  console.log('')

  var 正常结束 = false
  try {
    executeDailyTasks()

    // 多账号：切换区服重新执行
    if (isDailyEnabled('全部账号')) {
      while (nav(服务器选择)) {
        var server = 服务器选择Page.next()
        if (!server) break
        currentServer = server
        console.log('')
        console.log('--- 切换服务器: ' + server + ' ---')
        console.log('')
        sleep(800)
        // 不等待加载完成：切服加载期识别不可靠，直接交给 Router 的未知页面回退机制兜底
        executeDailyTasks()
        sleep(1000)
      }
      currentServer = null
    }
    正常结束 = true
  } finally {
    // 摘要与通知都由 taskReport/taskNotifier 在收到「整轮结束」时输出
    发布({ 类型: '整轮结束', 结果: 正常结束 ? '完成' : '已停止' })
  }
}
```

即：原末尾那 6 行摘要（`console.log('') / '====' / '   日常任务 — 完成' / '   成功: ...' / '===='`）**整段删除**。

- [ ] **步骤 3：验证 —— 构建 + 静态检查**

运行：

```bash
npm run build
grep -n "successTasks\|skipTasks\|failTasks\|totalTasks" src/model/daily.ts
```

预期：构建无报错；grep 无输出。

- [ ] **步骤 4：验证 —— 真机跑一轮日常（最关键的验收）**

**前置：先把 `dist/main.js` 推到设备**（306 KB，用「设备执行机制」第 1 步的 curl 上传，`name` 换成 `main.js` 的 URL 编码，并校验两边字节数一致）。

> **这一步必须用真实脚本运行方式，不能用 eval。** 整轮日常依赖无障碍服务、页面识别与 `ui` 线程，且要把结果落到详细日志文件；exec 上下文与真实运行不等价。
>
> 由 **k 操作**：在 AutoX.js 里打开 `/sdcard/脚本/xjskp/dist/main.js` 执行，或从项目入口启动。

1. 在设置页勾选「执行结果通知」（默认关闭）
2. 启动日常任务，跑完整轮
3. 预期：
   - 右上角计数条随任务推进实时跳动
   - 结束时通知栏出现 `日常完成 ✅N ⏭N ❌N`，展开含跳过/异常明细
   - 详细日志文件里的摘要为 `成功: N | 跳过: N | 异常: N | 合计: N`，且**合计恒等于三项之和**（旧版三处裸计数会导致合计偏小，这正是要看出来的差异）

4. 再点一次「启动」后立刻点小球「停止」，预期收到 `日常已停止 ✅N ⏭N ❌N` 的通知。

**若通知没出现**：先看日志里有没有 `[通知] 发送失败`，再去系统设置确认 AutoJs6/AutoX.js 的通知权限。这一步的实测结论要回写到规格文档的「待真机确认」清单。

- [ ] **步骤 5：Commit**

```bash
git add layout/layout.xml src/model/daily.ts
git commit -m "功能:runDaily 接入通知生命周期,摘要改由汇总模块输出"
```

---

### 任务 8：回归验收与规格回写

**文件：**
- 修改：`docs/superpowers/specs/2026-09-19-任务结果通知-design.md`（回写实测结论）

- [ ] **步骤 1：改动前后比对**

若改动前没有留存日志，用 `git stash` 回到改动前跑一轮、留存详细日志，再 `git stash pop`；已有留存日志则直接用。

改动后跑一轮（「全部账号」关闭与打开各一次），比对两轮日志。

预期差异**只有两处**：
1. 观影签到相关条数从 3 条降到 2 条
2. 原三处裸计数现在计入合计，合计比旧版大

除此之外每类任务的条数应逐一对得上。**若还有出入，就是 `doTask` 埋点漏了地方，回到任务 6 排查。**

- [ ] **步骤 2：回写规格文档的「待真机确认」**

把规格文档里 7 条待确认项逐条改成实测结论（删掉不确定表述，写上"实测：…"）。特别是：

- `<text>` 的自定义属性是否生效、最终采用的 `w`/`alpha`
- 通知权限的默认状态与 `ensureEnabled()` 的实际表现
- `notificationId: 1919` 是否与其它通知冲突

- [ ] **步骤 3：Commit**

```bash
git add docs/superpowers/specs/2026-09-19-任务结果通知-design.md
git commit -m "文档:任务结果通知设计回写真机实测结论"
```
