# Game.ts 适配新架构 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 `src/model/Game.ts`（废弃代码，57 个编译错误）适配到新架构（pages/ 页面对象 + utils/img.ts + Router 单例），恢复战斗主控功能并在 main.ts 接入。

**架构：** Game 保留状态机结构（GameStatus + start() 循环 + battleHandler），页面操作委托给新页面实例（战斗中/暂停战斗/战斗结束/选择技能）；准备流程用 Router 单例导航；main.ts 新增 UiGameConfig 从 UI 读值。精英掉落暂不处理，组队保留框架（退队/副本邀请检测 TODO）。

**技术栈：** TypeScript (es5 target) + Webpack 5，AutoJs6 运行时（Rhino 引擎，无箭头函数/const/解构）。

**参考：** 设计文档 `docs/superpowers/specs/2026-08-13-game-ts-适配新架构-design.md`

---

## 文件结构

| 文件 | 职责 | 动作 |
|------|------|------|
| `src/pages/战斗中.ts` | 新增 `hasUplayer()` 方法（暂停按钮像素变色检测） | 修改 |
| `src/model/Game.ts` | 战斗主控：状态机 + 循环 + 准备 + battleHandler | 重写（保留旧逻辑结构） |
| `src/main.ts` | `UiGameConfig` 从 UI 读值 + 启动按钮接入 Game | 修改 |
| `docs/superpowers/plans/2026-08-13-game-ts-适配新架构.md` | 本计划 | 创建 |

**构建验证：** `npm run build`（webpack production，唯一自动化验证；无 Node 单测，测试全部真机运行）。

---

### 任务 1：战斗中.ts 新增 hasUplayer

**文件：** 修改 `src/pages/战斗中.ts`（在 `等级()` 方法后、注释块 `// /**` 前插入）

- [ ] **步骤 1：新增 hasUplayer 方法**

在 `src/pages/战斗中.ts` 的 `等级()` 方法（第 62-74 行）之后插入：

```ts
  /** 暂停按钮处像素不再是按钮色 → 有上层窗口遮挡（暂停面板/弹窗会使按钮变色） */
  hasUplayer(img: ImageWrapper): boolean {
    let p = this.暂停_point
    return !!p && !colors.isSimilar(img.pixel(p.x + 18, p.y + 18), '#ffffff', 10)
  }
```

`暂停_point` 由 `is()` 写入（匹配点=模板左上角），+18 为模板中心估计值，真机验证项。

- [ ] **步骤 2：构建验证**

运行：`npm run build`
预期：0 错误（webpack compiled successfully）

- [ ] **步骤 3：Commit**

```bash
git add src/pages/战斗中.ts
git commit -m "战斗中:新增hasUplayer方法(暂停按钮像素变色检测上层窗口)"
```

---

### 任务 2：Game.ts 依赖与实例适配

**文件：** 修改 `src/model/Game.ts`

- [ ] **步骤 1：替换 import 区与类头部**

删除 `src/model/Game.ts` 第 1-7 行全部注释掉的 import，替换为：

```ts
import { GameConfig, GameType } from "../MainWindow"
import { smallWindow } from "../SmallWindows"
import { Router } from '../router/Router'
import { screen, width, height, toScreenX, toScreenY, tryCloseModals, select_队友, getTemplate } from '../utils/img'
import { skillStrategy } from '../utils/技能策略'
import { 战斗中 } from '../pages/战斗中'
import { 暂停战斗 } from '../pages/暂停战斗'
import { 战斗结束 } from '../pages/战斗结束'
import { 选择技能 } from '../pages/选择技能'
import { 寰球救援 } from '../pages/寰球救援'
import { 战斗 } from '../pages/战斗'
```

替换类头部（第 12-18 行）与字段区（第 20-25 行）：

```ts
export class Game {
  static img_元素试炼_挑战 = getTemplate("images/元素试炼_挑战.png")
  static img_元素试炼_开始游戏 = getTemplate("images/元素试炼_开始游戏.png")
  constructor(private gameConfig: GameConfig) { }

  private 战斗中Page = new 战斗中()
  private 暂停战斗Page = new 暂停战斗()
  private 战斗结束Page = new 战斗结束()
  private 选择技能Page = new 选择技能()
  private 寰球救援Page = new 寰球救援()
  private 战斗Page = new 战斗()

  private enable_倍速 = false
  private level = 0
  private runNum = 0
  private status: GameStatus = GameStatus.战斗结束
  private startTime = 0
```

要点：
- 删除 `colors_倍速_多点`、`colors_倍速`、`倍速_point`（多点找色废弃，改用模板检测）
- 删除 `img_寰球远征_准备`（旧代码未使用）
- `img_元素试炼_*` 用 `getTemplate`（带缓存与 imageBasePath 路径兜底），保留 static 字段

- [ ] **步骤 2：构建验证**

运行：`npm run build`
预期：错误数显著减少（剩余错误来自 倍速/currentLevel/battleHandler/准备/start 的旧符号引用，下任务处理）

- [ ] **步骤 3：Commit**

```bash
git add src/model/Game.ts
git commit -m "Game:import与页面实例适配新架构(删除倍速多点找色)"
```

---

### 任务 3：Game.ts 倍速 / 等级 / battleHandler

**文件：** 修改 `src/model/Game.ts`

- [ ] **步骤 1：替换倍速与等级方法**

替换旧 `倍速(img)` 方法（第 26-43 行）与 `currentLevel(img)` 方法（第 44-60 行）：

```ts
  倍速() {
    if (this.enable_倍速) return
    if (this.战斗中Page.已开15倍速()) {
      this.enable_倍速 = true
    } else {
      this.战斗中Page.开15倍速()
    }
  }
  currentLevel(): number {
    if (!this.level && this.战斗中Page.暂停_point) {
      this.level = this.战斗中Page.等级()
      console.log("ocr level: " + this.level)
    }
    return this.level
  }
```

要点：
- 倍速：`已开15倍速()` 找"15倍速-开启"模板（图标变橙=已开启，语义与旧"左侧橙色像素"一致）；未开启则 `开15倍速()` 点击
- 等级：`战斗中.等级()` 已封装 OCR（clip 暂停按钮右侧 + ocrRegion），Game 层保留 level 缓存；`暂停_point` 由循环条件里 `战斗中Page.is(img)` 写入
- 方法签名去掉 img 参数（新页面方法内部自己截图，screen() 500ms 缓存复用循环帧）

- [ ] **步骤 2：替换 battleHandler**

替换旧 `battleHandler`（第 73-107 行）：

```ts
  private battleHandler(img: ImageWrapper): boolean {
    if (this.选择技能Page.is(img)) {
      // 6秒内选择技能当成头选宝石效果，不提升等级
      this.选择技能Page.selectSkill(img, this.gameConfig.identifySkill) && Date.now() - this.startTime > 6 * 1000 && this.level && this.level++
      return true
    } else if (this.暂停战斗Page.is(img)) {
      log("暂停中")
      if (this.status == GameStatus.退出战斗) {
        this.暂停战斗Page.结束战斗()
      } else {
        this.暂停战斗Page.继续()
      }
      return true
    } else if (this.战斗结束Page.is(img)) {
      log("返回中")
      // 一局结束
      this.战斗结束Page.back()
      this.reset()
    } else {
      if (tryCloseModals()) {
        log("关闭弹窗")
      } else {
        click(width / 2, height - 10)
        log("尝试关闭战斗中未知窗口")
      }
    }
    return false
  }
```

要点：
- 选择技能：`selectSkill` 失败不点底部兜底（旧 `back()` 有重试+底部点击），返回 false 时下轮循环重试；`selectSkill` 内部 `is()` 未写入 `选择技能_point` 时 `entryPoints` 走 `找词组顶部` 用 `this.选择技能_point`——若为 undefined 会异常，由 `is(img)` 先匹配保证有值
- 暂停：`退出战斗` 状态点 `结束战斗()`（结算退出），否则 `继续()`
- 战斗结束：`back()` + `reset()`，**不 return true**（落回 return false，与旧行为一致：reset 后 status=战斗结束，循环条件退出）
- 重新连接与通用弹窗合并进 `tryCloseModals()`（含重新连接/跳过/关闭/确定模板 + 多点找色兜底）
- 精英掉落分支删除（暂不处理）

- [ ] **步骤 3：构建验证**

运行：`npm run build`
预期：错误仅剩准备流程与 start() 中的旧符号（PageRoute/page_XXX/screen.capture）

- [ ] **步骤 4：Commit**

```bash
git add src/model/Game.ts
git commit -m "Game:倍速/等级/battleHandler映射新页面对象"
```

---

### 任务 4：Game.ts 准备流程适配

**文件：** 修改 `src/model/Game.ts`

- [ ] **步骤 1：替换队长_准备 / 队员_准备**

替换旧 `队长_准备`（第 108-128 行）与 `队员_准备`（第 129-145 行）：

```ts
  队长_准备(): boolean {
    if (this.gameConfig.invite) {
      if (this.gameConfig.teammate == undefined) {
        toast("未选择队员")
        throw new Error("未选择队员")
      }
      // TODO: 退队检测(旧 退队_point)待补：已在队伍中时先退队
      // TODO: 组队邀请页对象未建(见 2026-08-12-组队邀请页面-design.md)，需先路由到邀请页；
      //       注意 select_队友 是死循环(找不到会无限滑动)，前置导航缺失时调用会卡死
      let point = select_队友(this.gameConfig.teammate)
      if (point) {
        click(toScreenX(point.x), toScreenY(point.y))
        sleep(2000)
      }
    }
    return false
  }
  队员_准备(): boolean {
    if (this.gameConfig.acceptInvite) {
      if (this.gameConfig.teammate == undefined) {
        toast("未选择队长")
        throw new Error("未选择队长")
      }
      // TODO: 副本邀请弹窗检测(旧 副本邀请_point)待补；select_队友 为死循环，弹窗未出现时调用会卡死
      let point = select_队友(this.gameConfig.teammate)
      if (point) {
        click(toScreenX(point.x), toScreenY(point.y))
        sleep(1200)
      }
    }
    return false
  }
```

要点：
- `select_队友` 是死循环（找不到目标队友会一直滑动），依赖前置的邀请页导航（TODO），真机验证项
- 旧"循环邀请直到离开组队页"逻辑移除（依赖已删除的 退队_point），TODO 标注

- [ ] **步骤 2：替换 prepare_* 四个方法**

替换旧 `prepare_精英关卡`（第 146-159 行）、`prepare_寰球救援`（第 160-176 行）、`prepare_元素试炼`（第 177-190 行）：

```ts
  prepare_精英关卡(): boolean {
    if (this.gameConfig.enableTeam) {
      if (this.gameConfig.isLeader) {
        if (this.队长_准备()) {
          return Router.getInstance().go(战斗中)
        }
      } else {
        if (!this.队员_准备()) {
          return Router.getInstance().go(战斗)
        }
      }
    }
    return true
  }
  prepare_寰球救援(): boolean {
    if (this.gameConfig.enableTeam) {
      if (this.gameConfig.isLeader) {
        if (!Router.getInstance().go(寰球救援)) {
          return false
        }
        if (this.队长_准备()) {
          return Router.getInstance().go(战斗中)
        }
      } else {
        if (!this.队员_准备()) {
          return Router.getInstance().go(寰球救援)
        }
      }
    }
    return true
  }
  prepare_元素试炼(): boolean {
    let point = images.findImageInRegion(screen(), Game.img_元素试炼_挑战,
      width * 0.3, height * 0.2, width * 0.4, height * 0.6)
    if (point) {
      click(toScreenX(point.x), toScreenY(point.y))
      sleep(500)
    }
    point = images.findImageInRegion(screen(), Game.img_元素试炼_开始游戏,
      width * 0.2, height * 0.6, width * 0.6, height * 0.3)
    if (point) {
      return click(toScreenX(point.x), toScreenY(point.y))
    }
    return false
  }
```

要点：
- `PageRoute.goto(page_X, 1, 1)` → `Router.getInstance().go(X)`（新 Router.go 仅一个类参数）
- `screen.capture()` → `screen()`；`screen.width/height` → import 的 `width/height`（1080x2376 缩放坐标）
- 未使用的 `战斗Page` 实例保留字段（队员非队长分支用 `Router.go(战斗)`，传类即可，实例供后续扩展）

- [ ] **步骤 3：构建验证**

运行：`npm run build`
预期：错误仅剩 start() 主循环中的旧符号

- [ ] **步骤 4：Commit**

```bash
git add src/model/Game.ts
git commit -m "Game:准备流程适配Router单例+组队兼容select_队友"
```

---

### 任务 5：Game.ts start() 主循环

**文件：** 修改 `src/model/Game.ts`

- [ ] **步骤 1：替换 start() 方法**

替换旧 `start()`（第 191-245 行）：

```ts
  start() {
    do {

      if (!this.战斗中Page.is(screen())) {
        // 战斗前准备
        if (this.status == GameStatus.战斗结束) {
          if (this.gameConfig.type == GameType.寰球救援) {
            this.prepare_寰球救援()
          } else if (this.gameConfig.type == GameType.精英关卡) {
            this.prepare_精英关卡()
          } else if (this.gameConfig.type == GameType.普通关卡) {
            this.gameConfig.enableStart && Router.getInstance().go(战斗中)
          } else if (this.gameConfig.type == GameType.元素试炼) {
            this.gameConfig.enableStart && this.prepare_元素试炼()
          } else {
            // 寰球远征_准备 等未支持模式：防死循环，提示后退出
            log("不支持的关卡类型: " + this.gameConfig.type)
            return
          }
        }
      }
      let img = screen()
      this.startTime = Date.now()
      let sleepTime = 2000
      while (this.status != GameStatus.战斗结束 || this.战斗中Page.is(img)) {
        if (this.战斗中Page.hasUplayer(img)) {
          if (this.battleHandler(img)) {
            log('战斗中上层窗口处理完成')
            this.status = GameStatus.战斗中
          }
        } else {
          this.status = GameStatus.战斗中
          if (this.gameConfig.timeOut && Date.now() - this.startTime > this.gameConfig.timeOut * 60 * 1000) {
            log('战斗超时 返回')
            this.status = GameStatus.退出战斗
            this.战斗中Page.暂停()
          }
          // 队长开启倍速
          if (this.gameConfig.isLeader) this.倍速()
          // 开启提前退出功能
          if (this.gameConfig.exitLevel) {
            // 如果提前退出等级为1直接退出，不识别当前等级;或者当前等级大于等于提前退出等级
            if (this.gameConfig.exitLevel == 1 || this.currentLevel() >= this.gameConfig.exitLevel) {
              log('提前退出')
              this.status = GameStatus.退出战斗
              this.战斗中Page.暂停()
              sleepTime = 800
            }
          }
        }
        sleep(sleepTime)
        img = screen()
      }
      if (this.status == GameStatus.战斗结束) {
        log('游戏次数', this.runNum)
      }

    } while (this.runNum != this.gameConfig.runNum && sleep(2000) == undefined)
  }
```

要点：
- `page_战斗中.is(screen.capture())` → `this.战斗中Page.is(screen())`
- `page_战斗中.hasUplayer(img)` → `this.战斗中Page.hasUplayer(img)`
- 超时/提前退出的 `page_战斗中.back()` → `this.战斗中Page.暂停()`（点暂停按钮弹面板，下轮 hasUplayer → battleHandler 点结束战斗）
- `this.倍速(img)` → `this.倍速()`；`this.currentLevel(img)` → `this.currentLevel()`
- **新增 else 分支**：`寰球远征_准备` 等未支持类型 log 后 return，防止外层 do-while 空转死循环（旧代码该模式会无限空转）
- 循环终止条件、runNum 计数、reset() 调用位置不变

- [ ] **步骤 2：构建验证**

运行：`npm run build`
预期：0 错误

- [ ] **步骤 3：检查残留**

运行：`grep -n "PageRoute\|page_\|screen.capture\|ocrPointFind\|ocr_zh\|colors_倍速" src/model/Game.ts`
预期：无输出（全部旧符号已清除）

- [ ] **步骤 4：Commit**

```bash
git add src/model/Game.ts
git commit -m "Game:start主循环适配新页面API+未支持模式防死循环"
```

---

### 任务 6：main.ts 接入 Game

**文件：** 修改 `src/main.ts`

- [ ] **步骤 1：新增 UiGameConfig 类**

在 `src/main.ts` 的 import 区追加 `Game`：

```ts
import { Game } from './model/Game'
```

在 `var 探索运行中 = false` 之后插入：

```ts
/** 从主窗口 UI 控件读取战斗配置 */
class UiGameConfig extends GameConfig {
  type: GameType
  enableStart: boolean
  constructor() {
    super()
    this.type = mainWindow.window.模式.getSelectedItem() as GameType
    this.runNum = mainWindow.window.执行次数.widget.getValue() || -1
    this.exitLevel = mainWindow.window.退出等级.widget.getValue() || 0
    this.timeOut = mainWindow.window.超时退出.widget.getValue() || 0
    this.enableStart = mainWindow.window.开始游戏.widget.isChecked()
    this.enableTeam = mainWindow.window.enable_组队.widget.isChecked()
    this.isLeader = mainWindow.window.队长.widget.isChecked()
    this.invite = mainWindow.window.自动邀请.widget.isChecked()
    this.acceptInvite = mainWindow.window.自动接受邀请.widget.isChecked()
    this.identifySkill = mainWindow.window.识别技能.widget.isChecked()
    // TODO: teammate 由"获取队友信息"流程注入(UI 按钮未绑定,待实现)
  }
}
```

- [ ] **步骤 2：替换启动按钮逻辑**

替换 `src/main.ts` 启动按钮 onClick 中 `tab==2` 分支之后的模式判断部分（即 `mainWindow.window.模式.getSelectedItem() === GameType.普通关卡` 的 if/else 块）为：

```ts
    // 功能页:按模式进入对应战斗
    start(function () {
      new Game(new UiGameConfig()).start()
    })
```

即删除旧 `mainWindow.window.模式.getSelectedItem() === GameType.普通关卡` 分支与 `else { start(runDaily) }`，以及已注释的 `//new 战斗中().自动战斗()`。

- [ ] **步骤 3：构建验证**

运行：`npm run build`
预期：0 错误

- [ ] **步骤 4：Commit**

```bash
git add src/main.ts
git commit -m "main:UiGameConfig从UI读值+启动按钮按模式接入Game"
```

---

### 任务 7：全量验证与真机清单

**文件：** 无代码变更

- [ ] **步骤 1：构建 + 残留检查**

运行：
```bash
npm run build
grep -rn "PageRoute\|screen.capture\|ocr_zh\|ocrPointFind" src/ | grep -v "//"
```
预期：build 0 错误；grep 无输出（src/ 下无旧符号残留）

- [ ] **步骤 2：真机验证清单（记录结果）**

| 项 | 验证方法 | 预期 |
|----|---------|------|
| 普通关卡战斗循环 | 模式=普通关卡 → 启动 | 进入战斗 → 倍速/等级/技能弹窗正常 → 战斗结束退出 → 按执行次数循环 |
| hasUplayer 偏移 | 战斗中暂停面板/技能弹窗出现 | 正确进入 battleHandler 对应分支(暂停+18 偏移需真机校准) |
| 提前退出 | 退出等级=5 | 等级 OCR 达到 5 后点暂停→结束战斗 |
| 超时退出 | 超时退出=1 | 1 分钟后点暂停→结束战斗 |
| 倍速 | 开启倍速(队长) | 战斗内 15 倍速开启,不重复点击 |
| 寰球救援/精英关卡单机 | 对应模式,不勾组队 | 直接进战斗循环 |
| 元素试炼 | 模式=元素试炼 | 点挑战→开始游戏→进战斗 |
| 寰球远征_准备 | 模式=寰球远征_准备 | log"不支持的关卡类型"后退出,不死循环 |
| 组队(半成品) | 勾组队+自动邀请,已在邀请页 | select_队友 找到队友点击(前置导航 TODO 未实现) |
| 日常 tab | tab=日常 → 启动 | runDaily 正常 |

---

## 风险与注意事项

- `hasUplayer` 偏移 +18 是估计值（旧代码 +5、模板尺寸不同），真机验证优先项
- `select_队友` 死循环：必须在组队邀请页调用，前置导航缺失（TODO），勾选组队且不在邀请页时会卡住——真机验证时注意
- Game new 页面实例会重复注册到 Router（register 不去重），仅增加几次 is() 调用，无功能影响
- `UiGameConfig` 的 `模式.getSelectedItem()` 返回 string，与 GameType 枚举值一一对应（layout entries 同源）
